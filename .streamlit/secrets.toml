### Gmail SMTP - Edit with your REAL credentials
# PLACEHOLDERS will cause DeltaGenerator crashes during st.secrets access!

[gmail]
sender_email = "techiehelp57@gmail.com"
sender_password = "ucgh ucqp xehr bssf"
smtp_server = "smtp.gmail.com"
smtp_port = 587

### 🚨 REQUIRED Setup (before send):
# 1. Enable Gmail 2FA: https://myaccount.google.com/security
# 2. Generate App Password: https://support.google.com/accounts/answer/185833
# 3. Replace placeholders above with REAL values
# 4. Restart app: Ctrl+C then `streamlit run app.py`
# 5. NEVER commit real secrets! (.gitignore auto-excludes)
#
# Test: App sidebar shows ✅ after fix instead of ❌

### Validation Added: App now handles invalid secrets gracefully without crash


